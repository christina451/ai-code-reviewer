/**
 * OpenRouterAIService.
 *
 * Concrete implementation of the AIService interface (see
 * /services/ai-service.ts), backed by OpenRouter's API. This is the
 * ONLY file that should know OpenRouter-specific request/response
 * shapes. Swapping providers later means writing a new file here, not
 * editing the service layer.
 *
 * Filled in during Milestone 7.
 */

export {};
