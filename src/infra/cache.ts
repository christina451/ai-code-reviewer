/**
 * Cache.
 *
 * Thin wrapper around Redis. Used to cache expensive results (e.g.
 * static analysis output for an unchanged file, or AI responses for
 * identical findings) so repeated requests don't redo work.
 *
 * Filled in during Milestone 10 (Redis caching layer).
 */

export {};
