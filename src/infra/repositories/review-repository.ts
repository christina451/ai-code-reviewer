/**
 * ReviewRepository.
 *
 * Defines how reviews and findings are persisted, as an interface, plus
 * the concrete Postgres implementation. Services depend on the
 * interface; this file is the only place that knows SQL exists.
 *
 * Filled in during Milestone 9 (repository pattern + Postgres wiring).
 */

export {};
