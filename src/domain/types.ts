/**
 * Domain layer.
 *
 * Pure TypeScript types describing the business domain: Review, Finding,
 * Severity, etc. No imports from Next.js, Postgres, Redis, or any AI SDK
 * are allowed in this folder. If a type here needs to import something
 * framework-specific, it belongs in /infra instead.
 *
 * Filled in during Milestone 6 (structured findings JSON schema).
 */

export {};
