# /analysis/rules

One file per deterministic static analysis rule, each operating on the
AST produced by /analysis/ast: cyclomatic complexity, function length,
nesting depth, duplicate code, unused variables, unreachable code,
TODO/FIXME detection, maintainability index.

Filled in during Milestones 4–5.
